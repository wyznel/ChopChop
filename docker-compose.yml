services:
  videosplitter:
    build: .
    container_name: chopchop
    ports:
      - "8080:8000"   # host 8080 -> container 8000
    volumes:
      - ./uploads:/uploads
      - ./app/static:/app/static:ro
    environment:
      - UPLOAD_ROOT=/uploads
    restart: unless-stopped
